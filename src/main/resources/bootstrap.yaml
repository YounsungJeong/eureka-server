server:
  port: 8761

spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: eureka-server/eureka-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false
    fetch-registry: false